# ── Seed (acesso direto ao banco via SQLAlchemy) ─────────────────────────────
# Caminho absoluto para o diretório raiz do svfinance-api
SVFINANCE_API_PATH=/home/salvatini/projetos/svfinance/svfinance-api

# Variáveis requeridas pelo create_app() do svfinance-api
DATABASE_URL=postgresql://...
JWT_SECRET_KEY=
SECRET_KEY=
RESEND_API_KEY=
DEV_MODE=True

# ── Testes de API (pytest + requests) ────────────────────────────────────────
# URL base da API alvo (sem barra final)
API_BASE_URL=https://api.svfinance.com.br/api

# Senha usada para o usuário QA criado pelo seed
QA_PASSWORD=SvFinanceQA#2026!
